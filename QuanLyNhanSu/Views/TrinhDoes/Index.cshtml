@model IEnumerable<QuanLyNhanSu.Models.TrinhDo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="margin-top:20px; margin-bottom: 20px">
    <h2 class="text-center">Quản lý trình độ</h2>
    <button class="btn btn-primary" onclick="showModal('@Url.Action("Create", "TrinhDoes")', 'Thêm trình độ')">
        <i class="bi bi-plus-circle"></i> Thêm trình độ
    </button>
</div>
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        Danh sách trình độ
    </div>
    <table class="table  table-bordered table-hover table-sm card-body text-center">
        <thead class="table-info">
            <tr>
                <th>STT</th>
                <th>Tên trình độ </th>
                <th style="width: 25%;">Hành động</th>
            </tr>
        </thead>
        <tbody>
            @{
                int index = 1;
            }
            @foreach (var item in Model)
            {
                <tr>
                    <td>@index</td>
                    <td>@Html.DisplayFor(modelItem => item.TenTD)</td>

                    <td text-nowrap p-1>
                        <div class="d-flex gap-1">

                            <a class="btn btn-warning btn-sm" onclick="showModal('@Url.Action("Edit", "TrinhDoes", new { id = @item.IdTD })', 'Chỉnh sửa thông tin trình độ')"> <i class="fas fa-edit"></i> Sửa</a>

                            <a asp-action="Delete" asp-route-id="@item.IdTD" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc chắn muốn xóa không?');"><i class="fas fa-trash"></i> Xóa</a>
                        </div>
                    </td>
                </tr>
                index++;
            }
        </tbody>
    </table>
</div>


